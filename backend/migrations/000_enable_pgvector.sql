-- ============================================================
-- pgvector 扩展启用脚本
-- ============================================================
-- 
-- 说明：
-- 1. 这是唯一需要手动执行的数据库迁移脚本
-- 2. 官方 LangChain PGVector 会自动创建所需的表结构：
--    - langchain_pg_collection (collection 元数据表)
--    - langchain_pg_embedding (向量和文档存储表)
-- 3. 首次初始化 PgVectorStoreWrapper 时，官方会自动创建表
-- 4. 但 pgvector 扩展需要手动启用（需要超级用户权限）
--
-- 执行步骤：
-- 1. 连接到你的数据库
-- 2. 执行本脚本启用扩展
-- 3. 验证扩展是否已启用（见下方验证查询）
--
-- ============================================================

-- 启用 pgvector 扩展
CREATE EXTENSION IF NOT EXISTS vector;

-- ============================================================
-- 验证扩展是否已启用
-- ============================================================
-- 执行以下查询验证：
-- SELECT 
--     extname, 
--     extversion,
--     n.nspname as schema
-- FROM pg_extension e
-- JOIN pg_namespace n ON e.extnamespace = n.oid
-- WHERE extname = 'vector';
--
-- 预期结果：应返回一行，显示 vector 扩展信息
-- ============================================================
