# 这个是与大模型聊天的管理系统

    ## 根据后端 API 定义，生成前端会话管理的代码，所有代码和配置放置在frontend下

技术栈：
vue3 + vite+ Tailwind CSS+TypeScript + Pinia ## 后端启动脚本：
uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000 ## 注意：尽量做简单实现，不要太复杂 ## 后端代码路径：D:\Python\workplace\rearch_agent\backend\app

## 优化 增加大模型的交互功能

根据后端的 api 定义，生成前端会话管理的代码，所有代码和配置放置在 frontend 下

- 增加流式输出和非流式输出切换的开关
