# 03 — 澄清操作生命周期闭环与端到端回归验证

**What to build:**
用户在点击“确认并恢复生成”后，本地即刻进入提交锁定与恢复生成状态，流式恢复后平滑过渡到完成态；用户点击“取消”时，状态正确落定为“已取消/已停止”。通过自动化测试验证状态机闭环与无视觉回退。

**Blocked by:** 02 — 子智能体卡片挂起等待态与标签优化

**Status:** done

- [x] 在 `MessageItem.vue` 和 `AskUserQuestionCard.vue` 之间验证并确保取消与恢复生成链路的状态完整性
- [x] 确保提交后按钮处于优雅的加载中锁定态，防止重复提交
- [x] 运行前端 `build:check` 类型检查与构建验证，确保所有状态流转正常无报错
- [x] 更新 `changelog.md` 记录修改内容与时间
