# 02 — 子智能体卡片挂起等待态与标签优化

**What to build:**
子智能体（如 `sql_domain_agent`）在执行 `AskUserQuestion` 挂起等待时，`SubagentCard.vue` 状态标签由“已中断（⚠️）”升级为“等待确认”，并搭配微光点和温和边框，消除用户在多智能体交互过程中因“已中断”文案而产生的系统报错或崩溃恐慌。

**Blocked by:** 01 — 消息气泡状态解耦与等待澄清 Banner 友好化

**Status:** done

- [x] 在 `SubagentCard.vue` 或其状态映射中，当子智能体处于等待澄清期时，将状态文案展示为“等待确认”
- [x] 优化等待确认态下的徽章图标与边框配色（从中性色/淡蓝色呼吸微光替代黄色警告感叹号）
- [x] 当子智能体遭遇真正的执行异常时，依然正确保留“执行失败”与红警示状态
