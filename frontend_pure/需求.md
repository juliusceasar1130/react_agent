# 这个是与大模型聊天的管理系统

    ## 根据后端 API 定义，生成前端会话管理的代码，纯三件套实现，frontend_pure 包括三个文件
        index.html
        main.js
        style.css
    ## 不使用额外的组件
    ## 后端启动脚本：
        uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000
    ## 注意：尽量做简单实现，不要太复杂
    ## 后端代码路径：D:\Python\workplace\rearch_agent\backend\app
